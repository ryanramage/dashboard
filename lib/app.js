
module.exports = {
    types: require('./types'),
    shows: require('./shows'),
    lists: require('./lists'),
    views: require('./views'),
    updates: require('./updates'),
    filters: require('./filters'),
    rewrites: require('./rewrites')
};

// Bind event handlers
// require('./events')
